---
- name: Debian Bootstrap for Local Pi Nodes
  hosts: local_pi_nodes
  gather_facts: true

  vars_files:
    - ../group_vars/vault.yml

  # Only ssh at local IP adress of machines at any port

  roles:
    - role: tailscale
